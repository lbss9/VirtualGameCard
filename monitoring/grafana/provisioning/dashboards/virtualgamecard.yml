apiVersion: 1

providers:
  - name: VirtualGameCard
    orgId: 1
    folder: VirtualGameCard
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
